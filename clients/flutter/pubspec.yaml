name: sharptown
description: Expressive Dart and Flutter client for the Sharptown image transformation API, with REST and JSON-RPC transports.
version: 0.1.0
repository: https://github.com/e1berd/sharptown
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.2.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
